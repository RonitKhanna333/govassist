{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "includeFiles": "{api/**,data/attributes.json,data/scripts/**,data/schemes/**/build/**,data/schemes/*/.state.json}"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.py" }]
}
